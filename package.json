{
  "name": "celesium-ai-site",
  "version": "2.0.0",
  "private": true,
  "description": "celesium.ai — applied AI studio brand site",
  "scripts": {
    "build": "node scripts/build-library.mjs && node scripts/smoke.mjs",
    "build-library": "node scripts/build-library.mjs",
    "smoke": "node scripts/smoke.mjs"
  },
  "dependencies": {
    "resend": "^4.0.0"
  },
  "devDependencies": {
    "linkedom": "^0.18.0"
  }
}
